let str = "A Waqas Rai is a bad Boy";

// get first index using the breaket notation and last using .length-1
let getLastIndex = str[str.length - 1];
console.log(`First Char ${str[0]} and Last Char ${getLastIndex}`);
