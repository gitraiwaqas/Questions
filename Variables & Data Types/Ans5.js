const pi = 3.14159;
console.log(pi);
pi = 4392489;
console.log(pi);

// what happen?
// Ans: It's provide an error. the error show we do not reassign the
// value to and constent variable if i need to reassign the value then we need a used a `let,var`
